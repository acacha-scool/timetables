<?php

if (! function_exists('seed_potential_lessons_from_curriculum')) {
    function seed_potential_lessons_from_curriculum() {
        //Traverse curriculum. It assumes curriculum data is seeded
    }
}